/**
 * Placeholder board-report shared types — impl 05 unblocks CI after
 * impl 07's commit (`9d10ecee`) re-exported `./board-report` from the
 * `packages/shared/src/reports/index.ts` barrel without the file this
 * import needed. Impl 06 (Board Report aggregation) owns the real type
 * contract (BoardReportRequest, BoardReportResponse, etc.) and will
 * replace this file when it lands.
 */

export {};
