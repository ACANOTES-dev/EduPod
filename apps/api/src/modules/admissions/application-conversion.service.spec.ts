// Deferred until the admissions rebuild lands. This file will be rewritten in
// Wave 2 when conversion becomes an automatic side-effect of the new state
// machine rather than a manual admin wizard. See
// new-admissions/implementations/05-conversion-to-student.md.
describe.skip('ApplicationConversionService', () => {
  it('is rewritten as part of the new admissions rebuild (Wave 2)', () => {
    expect(true).toBe(true);
  });
});
