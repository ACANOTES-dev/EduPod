// Deferred until the admissions rebuild lands. This file will be rewritten in
// Wave 2 when the new state machine, capacity gating, and form service ship.
// See new-admissions/implementations/03-state-machine-rewrite.md.
describe.skip('ApplicationsService', () => {
  it('is rewritten as part of the new admissions rebuild (Wave 2)', () => {
    expect(true).toBe(true);
  });
});
