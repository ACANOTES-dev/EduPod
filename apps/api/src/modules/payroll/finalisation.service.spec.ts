// Placeholder for the FinalisationService spec — the service itself is built
// in payroll-overhaul Wave 2 (impl 02). Wave 1 lands the shared types and
// schema this spec will exercise; flipping the describe.skip to describe()
// in Wave 2 is the trigger that lights up the suite.

describe.skip('FinalisationService', () => {
  it('implemented in payroll-overhaul Wave 2 (impl 02)', () => {
    // intentional placeholder
  });
});
