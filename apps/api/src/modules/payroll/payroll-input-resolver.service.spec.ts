// Placeholder for the PayrollInputResolver spec — the service itself is built
// in payroll-overhaul Wave 2 (impl 02). The resolver assembles a full
// CalcInput (defined in @school/shared/payroll) from every input source the
// audit found going unread; this spec will assert that nothing is missed.

describe.skip('PayrollInputResolver', () => {
  it('implemented in payroll-overhaul Wave 2 (impl 02)', () => {
    // intentional placeholder
  });
});
