/**
 * MessagingPolicyService — unit tests.
 *
 * Stub placeholder. The full service (and its tests) land in Wave 2,
 * Implementation 02 — "Messaging policy engine". See
 * `new-inbox/implementations/02-messaging-policy-engine.md`.
 *
 * This file exists so Jest's test discovery does not complain about the
 * empty module and so `turbo run test` surfaces the pending work.
 */
describe.skip('MessagingPolicyService (pending — Wave 2 / Impl 02)', () => {
  it('placeholder — implemented in Wave 2', () => {
    expect(true).toBe(true);
  });
});
