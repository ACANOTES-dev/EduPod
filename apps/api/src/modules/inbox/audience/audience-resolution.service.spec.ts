/**
 * AudienceResolutionService — unit tests.
 *
 * Stub placeholder. The full service (and its tests) land in Wave 2,
 * Implementation 03 — "Audience engine v2". See
 * `new-inbox/implementations/03-audience-engine-v2.md`.
 */
describe.skip('AudienceResolutionService (pending — Wave 2 / Impl 03)', () => {
  it('placeholder — implemented in Wave 2', () => {
    expect(true).toBe(true);
  });
});
