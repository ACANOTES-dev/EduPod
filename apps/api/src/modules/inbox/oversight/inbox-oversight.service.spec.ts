/**
 * InboxOversightService — unit tests.
 *
 * Stub placeholder. The full service (and its tests) land in Wave 2,
 * Implementation 05 — "Admin oversight service". See
 * `new-inbox/implementations/05-admin-oversight-service.md`.
 */
describe.skip('InboxOversightService (pending — Wave 2 / Impl 05)', () => {
  it('placeholder — implemented in Wave 2', () => {
    expect(true).toBe(true);
  });
});
