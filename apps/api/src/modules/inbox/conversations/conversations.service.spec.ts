/**
 * ConversationsService — unit tests.
 *
 * Stub placeholder. The full service (and its tests) land in Wave 2,
 * Implementation 04 — "Conversations + messages service". See
 * `new-inbox/implementations/04-conversations-messages-service.md`.
 */
describe.skip('ConversationsService (pending — Wave 2 / Impl 04)', () => {
  it('placeholder — implemented in Wave 2', () => {
    expect(true).toBe(true);
  });
});
