// Re-exports kept for back-compat. The canonical components live in
// `@/components/kpi-tile` so other hubs can import them without reaching
// into the people module's page-local `_components` folder.
export { CardSkeleton, KpiTile, UtilisationBadge } from '@/components/kpi-tile';
